// texturetool.cpp : 定义控制台应用程序的入口点。
//

#include "stdafx.h"

using namespace std;

const int ALLSIZE = 11 * 11;

struct wh{
	int w;
	int h;
};

array<wh, ALLSIZE> allSizes;	//所有可能的尺寸
vector<wh> outputlist;

int _tmain(int argc, _TCHAR* argv[])
{
	int i = 0;
	while (i++ < ALLSIZE){
		allSizes[i] = 
	}
	return 0;
}

